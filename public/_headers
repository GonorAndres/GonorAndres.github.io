# Cloudflare Pages header rules.
#
# This file is only interpreted by Cloudflare Pages, which in this repo hosts
# ONLY the dev and preview environments (gonorpage-dev.pages.dev). Production
# runs on GitHub Pages, which serves this file as a plain static asset and
# ignores its contents. That asymmetry is deliberate: a blanket noindex here
# keeps every dev/preview deployment out of search results without needing an
# environment variable, a build flag, or a conditional in astro.config.mjs.
#
# If production ever moves to Cloudflare Pages, this file MUST be reworked
# first, or gonor.me will be deindexed. See docs/DEPLOYMENT.md.

/*
  X-Robots-Tag: noindex, nofollow
